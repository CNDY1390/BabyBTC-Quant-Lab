{
  "buildCommand": "cd frontend && npm install && npm run build && cd ../landingpage && npm install && npm run build && cd .. && mkdir -p public && cp -r frontend/dist/* public/ && mkdir -p public/landingpage && cp -r landingpage/dist/* public/landingpage/",
  "outputDirectory": "public",
  "framework": null,
  "rewrites": [
    {
      "source": "/landingpage",
      "destination": "/landingpage/index.html"
    },
    {
      "source": "/landingpage/(.*)",
      "destination": "/landingpage/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/$1"
    }
  ]
}
